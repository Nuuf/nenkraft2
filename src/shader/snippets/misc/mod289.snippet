float mod289( float n ) {
     return ( n - floor( n * ( 1.0 / 289.0 ) ) * 289.0 );
}
vec2 mod289( vec2 p ) {
     return ( p - floor( p * ( 1.0 / 289.0 ) ) * 289.0 );
}
vec3 mod289( vec3 p ) {
     return ( p - floor( p * ( 1.0 / 289.0 ) ) * 289.0 );
}